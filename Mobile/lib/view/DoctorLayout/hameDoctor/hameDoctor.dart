import 'package:flutter/material.dart';

class hameDoctor extends StatelessWidget {
  const hameDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text("تم انشاء حساب "),
          ],
        ),
      ),
    );
  }
}
